---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header
  class="border-border-subtle bg-background-soft flex h-16 items-center justify-between border-b px-6"
>
  <h1 class="text-text-primary text-lg font-semibold">{title}</h1>
</header>
